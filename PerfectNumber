/*The objective of this coding exercise is to write a small program that determines if a number is a perfect number.*/


public class PerfectNumber {
    public static boolean isPerfectNumber (int number){
        if (number >=1){
            int sum=0;
            for (int i = 1; i<number;i++){
                if (number%i==0){
                    sum+=i;
                }              
                if (sum==number){
                    return true;
                }
            }
        }
        return false;
    }
}
